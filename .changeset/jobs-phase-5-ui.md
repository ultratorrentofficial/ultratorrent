---
'@ultratorrent/frontend': minor
---

Unified Jobs Center Phase 5 — the Jobs Center UI. A new Jobs Center in the System
workspace at /jobs: a real-metrics Overview, a single shared route-driven job list
(per-status tabs + search + server-side pagination + capability-gated row actions +
bulk cancel/retry/rerun with partial results), and a Detail view (status/progress,
sanitized error, parent/child relationships, live event timeline). All live via
polling. New typed `jobs` i18n namespace with full en-US/es-PR parity.
