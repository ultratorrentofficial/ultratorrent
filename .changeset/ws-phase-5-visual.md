---
'@ultratorrent/frontend': minor
---

nav(Workspace Phase 5): visual hierarchy, breadcrumbs, motion. The workspace
sidebar leads with the workspace's identity (icon + name → Overview) instead of
the logo, and its redundant group header is suppressed. Breadcrumbs root at the
workspace and link it to its Overview. A subtle, motion-reduce-safe cross-fade
plays on workspace change.
