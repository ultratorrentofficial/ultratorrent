# Base URL for the REST API. Defaults to http://localhost:4000/api when unset.
VITE_API_URL=http://localhost:4000/api

# Base URL for the Socket.IO server. Defaults to http://localhost:4000 when unset.
VITE_WS_URL=http://localhost:4000
