{
  "$comment": "Single source of truth for UltraTorrent versions (SemVer). `version` is the product line; per-edition versions track the repos: community (public UltraTorrent) and sdk (public UltraTorrent-SDK = packages/shared contracts). Managed by `npm run version:*`; consumed by GET /api/system/version.",
  "version": "0.25.0",
  "editions": {
    "community": "0.25.0",
    "sdk": "0.25.0"
  }
}
